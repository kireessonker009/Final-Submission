# Server settings
server.port=8080

# Logging level for better debugging (optional)
logging.level.org.springframework.web=DEBUG

# Application-specific settings (customize as needed)
app.name=My Web Application
app.version=1.0
